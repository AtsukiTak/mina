type Query {
	"""
	ユーザーをIDで検索する
	"""
	user(id: String!): User!
}
type User {
	id: String!
	name: String
}
type Mutation {
	signupAsAnonymous: UserAndSecret!
}
type UserAndSecret {
	user: User!
	secret: String!
}
schema {
	query: Query
	mutation: Mutation
}
