steps:
  - name: 'asia.gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'asia.gcr.io/${PROJECT_ID}/mina-backend:${SHORT_SHA}'
      - '.'
  - name: 'asis.gcr.io/cloud-builders/docker'
    args:
      - "push"
      - "gcr.io/${PROJECT_ID}/mina-backend:${SHORT_SHA}"
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'mina-backend'
      - '--image'
      - 'gcr.io/${PROJECT_ID}/mina-backend:${SHORT_SHA}'
      - '--platform'
      - 'managed'
      - '--region'
      - 'asia-northeast1'
      - '--allow-unauthenticated'
