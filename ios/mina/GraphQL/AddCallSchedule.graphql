mutation AddCallSchedule($input: AddCallScheduleInput!) {
    addCallSchedule(input: $input) {
        id
        callSchedules {
            id
            time
            weekdays
        }
        nextCallTime
    }
}
